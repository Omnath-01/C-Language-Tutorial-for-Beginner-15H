

#include <stdio.h>
int main()
{
    int a;

    printf("Enter the a : ");
    scanf("%d", &a);
    // one liner if statment
    (a < 5) ? printf("A is less than 5") : printf("A is not less than 5");

    return 0;
}
