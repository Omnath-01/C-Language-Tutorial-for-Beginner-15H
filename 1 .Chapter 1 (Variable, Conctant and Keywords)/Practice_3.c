// Program to conversion of temperature (centigrade degree temperaature to fahrenheit )

#include <stdio.h>
int main()
{
    float celsius = 37;
    float far = (celsius * 9 / 5) + 32;
    printf("The value of this celsius in Fahrenheit is %f : ", far);
    return 0;
}